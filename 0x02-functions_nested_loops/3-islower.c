#include "main.h"
/**
 * _islower - Function that checks for lowercase character
 * Return: 1 if c is lowercase or 0 else
 */
int _islower(int c)
{
if (_islower(c))
{
return (1);
}
else
{
return (0);
}
}
