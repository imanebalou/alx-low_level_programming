#include "main.h"
/**
 * _abs - Function that computes the absolute value of an integer
 * Return: absolute value of an integer
 */
int _abs(int)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
